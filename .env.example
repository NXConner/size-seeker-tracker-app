# Example environment variables for Size Seeker Tracker

# Strong, random 32+ character key for AES-GCM encryption used by secureStorage
# Generate one, e.g. with: openssl rand -base64 48
VITE_SECURE_STORAGE_KEY=

# Optional API base URL if you add remote integrations
VITE_API_URL=